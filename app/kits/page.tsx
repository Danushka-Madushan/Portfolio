/** 
 * each tool must have own route
 * kist/bcrypt
 * kits/uuid 
 * and each toolpage must have seo metadata
 * each page must be crawlable
 * add all of them to sitemap.xml
*/

const page = () => {
  return (
    <pre>Code to generate above output</pre>
  )
}

export default page
